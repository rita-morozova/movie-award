import React from 'react'
import ReactLoading from 'react-loading'

const LoadingComponent = () => (
  <ReactLoading type='spin' color='#cb2d6f' height={200} width={75} className='spin' />
)

export default LoadingComponent